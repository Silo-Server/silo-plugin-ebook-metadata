# silo-plugin-ebook-metadata

Standalone Silo metadata provider for ebook items. It is scaffolded for ISBN- and author-based ebook metadata and exposes capability `metadata_provider.v1` with ID `ebook-metadata`.
